{
  "installCommand": "npm install",
  "buildCommand": "npm run build:talentpool",
  "outputDirectory": "apps/talentpool/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}

